package Lab_excersise;

public class Lab002_ShortcutHW {
    public static void main(String[] args) {

//    IntelliJ shortcuts : https://resources.jetbrains.com/storage/products/intellij-idea/docs/IntelliJIDEA_ReferenceCard.pdf

//    Java Shorthands IntelliJ :

    /* In IntelliJ IDEA, shorthand abbreviations (known as **live templates**) help you quickly write common Java constructs.

            ---

            ### **General Java Shorthands**

            | Abbreviation | Expands To                                                              |
            |--------------|---------------------------------------------------------                |
            | `sout`       | `System.out.println();`                                                 |
            | `soutv`      | `System.out.println("var = " + var);` (prints variable)                 |
            | `soutp`      | `System.out.println("param = " + param);` (prints parameters)           |
            | `psvm`       | `public static void main(String[] args) {}`                             |
            | `fori`       | A for loop: `for (int i = 0; i < ; i++) {}`                             |
            | `iter`       | Enhanced for loop: `for (Type item : collection) {}`                    |
            | `itar`       | For loop iterating an array: `for (int i = 0; i < array.length; i++) {}`|
            | `ifn`        | `if (variable == null) {}`                                              |
            | `inn`        | `if (variable != null) {}`                                              |
            | `null`       | Inserts `null`.                                                         |

            ---

            ### **Error and Exception Handling**

            | Abbreviation | Expands To                                              |
            |--------------|---------------------------------------------------------|
            | `try`        | `try { } catch (Exception e) { e.printStackTrace(); }`  |
            | `tryf`       | `try (resource) { }` (try-with-resources)               |
            | `thr`        | `throw new Exception();`                                |
            | `throws`     | `throws Exception`                                      |

            ---

            ### **Methods and Classes**

            | Abbreviation | Expands To                                              |
            |--------------|---------------------------------------------------------|
            | `main`       | `public static void main(String[] args) {}`             |
            | `priv`       | `private`                                               |
            | `prot`       | `protected`                                             |
            | `pubf`       | `public final`                                          |

            ---

            ### **Logging**

            | Abbreviation | Expands To                                              |
            |--------------|---------------------------------------------------------|
            | `log`        | `private static final Logger LOG = LoggerFactory.getLogger(CurrentClass.class);` |
            | `logd`       | `LOG.debug();`                                          |
            | `logi`       | `LOG.info();`                                           |
            | `loge`       | `LOG.error();`                                          |

            ---

            ### **Other Useful Templates**

            | Abbreviation | Expands To                                              |
            |--------------|---------------------------------------------------------|
            | `list`       | `List<Type> list = new ArrayList<>();`                  |
            | `map`        | `Map<KeyType, ValueType> map = new HashMap<>();`        |
            | `not`        | `!` (logical NOT)                                       |
            | `ser`        | `private static final long serialVersionUID = 1L;`      |

            ---

            ### How to View or Customize Live Templates
1. **Access Live Templates:**
    Go to `File > Settings > Editor > Live Templates`.

            2. **Edit or Add Custom Templates:**
            - You can edit existing templates or create your own.
   - Assign a shorthand, define the expanded text, and set the applicable contexts (e.g., Java).

   */
    }
}
